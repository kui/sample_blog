- @title = 'Welcome'

- content_for :include do
  = feed_tag(:rss, url(:posts, :index, format: :rss), :title => "RSS")
  = feed_tag(:atom, url(:posts, :index, format: :atom), :atom => "RSS")

#posts= partial 'posts/post', collection: @posts
